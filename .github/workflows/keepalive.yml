name: keepalive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API de Render (evita spin-down del free tier)
        run: curl -fsS --max-time 55 https://libreria-api-v9h0.onrender.com/api